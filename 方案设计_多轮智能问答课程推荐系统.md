# 多轮智能问答方案设计文档（课程学习系统 · 售课咨询场景）

> 文档版本：v1.0
> 撰写日期：2026-08-02
> 配套文档：`调研报告_多轮智能问答开源项目.md`（技术调研部分）

---

## 目录

1. [项目背景与目标](#一项目背景与目标)
2. [场景定义与意图分组](#二场景定义与意图分组)
3. [技术栈选型](#三技术栈选型)
4. [参考项目](#四参考项目)
5. [整体架构](#五整体架构)
6. [核心机制：每轮五步处理管线](#六核心机制每轮五步处理管线)
7. [会话状态：需求画像](#七会话状态需求画像)
8. [信息收集策略：三种约束来源](#八信息收集策略三种约束来源)
9. [匹配与检索策略](#九匹配与检索策略)
10. [业务服务层：收窄过程与匹配结果中的核心帮助](#十业务服务层收窄过程与匹配结果中的核心帮助)
11. [工具清单与职责](#十一工具清单与职责)
12. [双层多轮：内部推理循环与可视化](#十二双层多轮内部推理循环与可视化)
13. [最终答案生成：查询产物 → 输出形态](#十三最终答案生成查询产物--输出形态)
14. [完整演示剧本](#十四完整演示剧本)
15. [业界共识五板斧对照](#十五业界共识五板斧对照)
16. [数据分层与存储](#十六数据分层与存储)
17. [风险与边界](#十七风险与边界)
18. [Demo 里程碑拆分](#十八demo-里程碑拆分)

---

## 一、项目背景与目标

### 1.1 背景

我们有一个课程学习系统（以英语教学为例）。用户（学生/家长）通过对话咨询课程：从"我想学英语"开始，逐步表达"基础差""一个月内能学完""多少钱""有套餐吗"等需求。系统需要在一个**多轮对话**中逐步收窄候选范围，最终给出精准的课程/套餐推荐与报价，引导用户下单。

### 1.2 核心命题

**在多轮问答场景下，如何提高答案的准确性？**

答案不是第一轮就给全，而是**随对话逐轮累积而变准**。多轮准确率提升的三大支柱：

1. **查询改写/指代消解**：解决"那要多少钱？"这类追问的指代与省略问题
2. **约束累积 + 记忆分层**：累积结构化约束集（而非冻结候选结果集），候选范围自动收窄
3. **结构化与文本分离检索**：价格/时长等结构化数据走参数化 SQL，课程内容等自由文本走向量检索

### 1.3 双层"多轮"定义

本 demo 要展示的"多轮"包含两个层面，缺一不可：

- **外层多轮（用户 ↔ AI）**：用户一句、AI 一句，逐轮补齐需求约束
- **内层多轮（AI ↔ 工具）**：AI 对一句话在内部自规划、自循环多次调用工具后才回复，且**全程可视化**

---

## 二、场景定义与意图分组

意图分组 = 场景路由。把用户粗问题映射到预定义业务场景，每个场景对应一条独立业务链。意图分组错了，后续全部动作都会错。

### 2.1 六类意图/场景

| 意图/场景 | 用户话术示例 | 走哪条业务链 |
|---|---|---|
| `course_recommend` 课程推荐 | "我想学英语，有什么课？" | 需求画像 + 课程匹配 |
| `price_query` 价格咨询 | "那要多少钱？" | 定价查询（参数化 SQL） |
| `content_query` 内容咨询 | "这门课讲什么？" | 知识库内容检索（向量） |
| `package_query` 套餐优惠 | "有没有套餐？" | 套餐匹配 + 性价比计算 |
| `plan_query` 学习规划 | "一个月能学完吗？" | 时长/周期匹配 |
| `chitchat` 闲聊/其他 | "你好"、"谢谢" | 不检索，直接对话 |

### 2.2 场景→工具→数据 映射关系

| 场景 | 使用的工具 | 数据源 | 检索类型 |
|---|---|---|---|
| 课程推荐 | 课程检索工具 | 课程表 + 课程摘要向量 | SQL + 向量混合 |
| 价格咨询 | 价格查询工具 | 定价表/套餐表 | 参数化 SQL |
| 内容咨询 | 内容检索工具 | 章节/单元/段落向量 | 向量 |
| 套餐优惠 | 套餐匹配工具 | 套餐表 + 套餐-课程映射 | 参数化 SQL + 计算 |
| 学习规划 | 时长匹配工具 | 课程时长 + 套餐时长 | 参数化 SQL |
| 闲聊 | 无 | 无 | 无 |

---

## 三、技术栈选型

### 3.1 技术栈清单

| 环节 | 选型 | 说明 |
|---|---|---|
| 后端 | **Java + Spring AI**（推荐引擎/工具调用/流式） | 与团队 Java 技术栈一致 |
| 前端 | **Ant Design X** 组件库（对话流/思维链/步骤展示/推荐卡片）+ SSE 流式 | 蚂蚁开源 AI 原生组件库 |
| 生成模型 | **DeepSeek-V4-Flash**（官方 API，OpenAI 兼容） | 输入 1 元/百万 token、输出 2 元，Agent 能力强（工具调用评测接近 Opus-4.8） |
| 理解模型 | **DeepSeek-V4-Flash**（同一模型，非思考模式做改写/意图/状态抽取） | 价格极低，无需再分大小模型 |
| Embedding | **BAAI/bge-m3**（硅基流动 SiliconFlow 在线 API，**免费**） | 1024 维、8192 上下文、中英双语；备选：本地部署 bge-m3（零成本） |
| 向量库 | **ChromaDB**（demo，Spring AI 原生支持，本地嵌入式） | 生产可换 pgvector/Milvus |
| 关系库 | **SQLite**（demo）/ MySQL（生产） | 通过 Spring Data JPA/MyBatis 抽象 |
| 会话存储 | 关系库存需求画像 + 消息历史 | session_id 维度 |
| Agent 编排 | Spring AI Function Calling / Spring AI Alibaba ReactAgent | 支持工具调用循环 + 事件回调 |

### 3.2 模型策略：单模型 + 模式切换

DeepSeek-V4-Flash 一个模型承担全部角色，不再分大小模型：

- **理解任务**（查询改写、意图分类、状态抽取）：用**非思考模式**调用，便宜且快
- **生成任务**（最终回答）：用**思考模式**调用，质量优先
- 选择它的理由：输入 1 元/百万 token、输出 2 元（缓存命中输入 0.02 元），Agent 工具调用能力评测得分接近 Opus-4.8，一个模型在成本和质量上同时满足"理解 + 生成"
- 预留扩展：若未来需要更高质量生成，可配置切到 DeepSeek-V4-Pro（输入 3 元/输出 6 元），代码层面通过统一 ChatClient 抽象，模型名可配

### 3.3 模型与向量接入策略

- `.env` / 配置文件统一管理 API Key
- LLM：DeepSeek 官方 API（`https://api.deepseek.com`，OpenAI 兼容格式），Spring AI 直接适配
- Embedding：硅基流动 SiliconFlow（`https://api.siliconflow.cn`，OpenAI 兼容 embeddings 接口）调用 **BAAI/bge-m3，免费**；若想完全离线，可本地跑 bge-m3（需 GPU/内存，demo 不必要）
- 供应商做抽象层，支持随时切换 OpenAI / 本地 Ollama

---

## 四、参考项目

完整调研见 `调研报告_多轮智能问答开源项目.md`，核心参考摘要：

| 项目 | 借鉴点 |
|---|---|
| [xqs-xqs/adaptive-rag-course](https://github.com/xqs-xqs/adaptive-rag-course) | 场景最贴近（选课 RAG）：意图分类四路路由、双层索引、混合检索 + RRF、parent-child chunking、多轮会话 |
| [infiniflow/RAGFlow](https://github.com/infiniflow/ragflow) | 深度文档理解（DeepDoc）、模板化分块、多路索引（全文+向量）、Rerank、Agent 工作流 |
| [langgenius/Dify](https://github.com/langgenius/dify) | 多轮对话 + 知识检索 + 工具调用的可视化编排 |
| [TencentCloud/Youtu-RAG](https://github.com/TencentCloud/Youtu-RAG) | 双层记忆（短期会话 + 长期跨会话）、Agent 自主检索决策 |
| [nageoffer/ragent](https://github.com/nageoffer/ragent) | 会话记忆（最近 N 轮 + 滚动摘要）、问题重写、混合检索工程化 |
| [HKUDS/LightRAG](https://github.com/HKUDS/LightRAG) | 轻量 RAG、流式多轮、上下文压缩 |
| Alhena AI（闭源） | 多轮查询改写四层并行（Contextualizer），60% 追问含指代问题 |
| 腾讯云 Agent Memory | 四层记忆架构（L0 原文 → L1 原子 → L2 场景 → L3 画像），白盒溯源 |
| [SURESHBEEKHANI/LangGraph-End-to-End](https://github.com/SURESHBEEKHANI/LangGraph-End-to-End) | SelfRAG / CorrectiveRAG / AdaptiveRAG 方法论 |
| [karthik-codex/Autogen_GraphRAG_Ollama](https://github.com/karthik-codex/Autogen_GraphRAG_Ollama) | 全本地多智能体方案参考 |
| [DevYangJC/Argus](https://github.com/DevYangJC/Argus) | Spring AI Alibaba 落地参考 |

---

## 五、整体架构

```
┌────────────────────────────────────────────────────────────┐
│ 前端：Ant Design X 聊天界面（SSE 流式）                       │
│  ├─ 对话消息流                                               │
│  ├─ 思考过程面板（工具调用轨迹可视化）                         │
│  └─ 已选课程/数据（页面选数据 → 预填参数）                    │
└──────────────────────────┬─────────────────────────────────┘
                           │ /api/chat (session_id, user_msg, selected_data?)
┌──────────────────────────▼─────────────────────────────────┐
│ Spring AI 后端                                               │
│  ├─ 对话编排层（五步处理管线，Agent 循环）                    │
│  │    ① 查询改写  ② 意图分类  ③ 状态更新                     │
│  │    ④ 按需检索执行（工具循环）  ⑤ 生成回答                  │
│  ├─ 会话状态管理（需求画像 + 消息历史 + 滚动摘要）            │
│  ├─ 工具层（预写查询函数：课程/价格/套餐/内容/时长/收口）      │
│  ├─ 结构化库（课程/章节/单元/段落/定价/套餐）                │
│  └─ 向量库（段落级 chunk，metadata 上卷到课程）               │
└──────────────────────────┬─────────────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │ LLM：DeepSeek-V4-Flash  │
              │      Embedding：bge-m3   │
              │     （SiliconFlow 免费） │
              └─────────────────────────┘
```

---

## 六、核心机制：每轮五步处理管线

每轮用户消息进来，都走五步——**理解必做，检索按需**。

### 6.1 ① 查询改写（指代消解）

把「历史对话 + 当前问题」交给模型（非思考模式，便宜快），重写成一条**自包含的完整问题**。

- 例：用户第 3 轮说"那要多少钱？" → 改写为"雅思基础班的课程价格是多少？"
- 原因：多轮对话 60%+ 追问带指代或省略，不改写直接检索会全部落空（Alhena 数据）

### 6.2 ② 意图分类（场景路由）

判断本轮意图，路由到对应业务链（见第二节），并决定检索深度：

- `chitchat` → 不检索
- `price_query` / `package_query` / `plan_query` → 只查结构化库（浅）
- `content_query` → 只走向量（浅）
- `course_recommend` → SQL + 向量都跑（深），复杂度高时触发子查询分解

### 6.3 ③ 状态更新（约束累积）

LLM 从本轮话里抽取新约束，累积进需求画像（见第七节）。

- 例："一个月内能学完吗" → 画像新增 `time_limit_days: 30`
- 例："500 以内" → 画像新增 `budget: 500`

### 6.4 ④ 检索执行（工具循环，按需）

用「改写后的问题 + 完整画像」执行工具：

- 结构化约束（科目/年级/时长/预算）→ **参数化查询**（AI 填参数，SQL 模板人工预写）
- 语义内容（"从零开始""基础差"等软需求）→ **向量检索**
- 该步骤是内层多轮：AI 可反复调用多个工具直至信息齐备（见第十二节）

### 6.5 ⑤ 生成回答（带引用）

把「改写问题 + 检索结果 + 需求画像 + 历史摘要」交给强模型，输出：

- 推荐课程/套餐 + 理由 + 价格 + 引用来源（课程名/章节名）
- 若约束不足，输出**引导性追问**（见第八节）

---

## 七、会话状态：需求画像

> 业界共识：**累积的是"约束集"，不是"结果集"**。候选集随约束动态收窄，而不是第一轮冻结。

### 7.1 画像结构（JSON）

```json
{
  "target_subject": "英语",
  "target_course": "雅思基础班",
  "level": "零基础",
  "grade": "高中",
  "is_after_school": true,
  "time_limit_days": 30,
  "budget": null,
  "learning_goal": "出国留学",
  "recommended_courses": ["雅思基础班"],
  "selected_data": { ... },
  "history_summary": "..."
}
```

### 7.2 画像生命周期

| 阶段 | 状态 | 触发条件 |
|---|---|---|
| 初始化 | 空画像 | 新会话开始，或页面选数据预填 |
| 收集中 | 约束逐步补齐 | 每轮状态更新 |
| 就绪 | 关键约束集齐 | 触发收口工具 |
| 收口 | 输出最终方案 | 生成推荐 + 报价 |

### 7.3 最小完整约束集（触发收口的阈值）

- **必须**：目标方向（target_subject/course）+ 当前水平（level）+ 时间周期（time_limit_days）
- **可选**：预算（budget）、人群（grade）、学习目标（learning_goal）
- 关键约束集齐 → **立刻触发收口工具**，不设固定轮数
- 未集齐但有强信号 → 先给"初步推荐 + 剩余待确认问题"

### 7.4 记忆分层（借鉴 Youtu-RAG / 腾讯 Agent Memory）

- **短期记忆**：本会话最近 N 轮完整保留 + 更早的滚动摘要（LLM 压缩，控 token 成本）
- **长期记忆**（可扩展）：跨会话沉淀用户偏好画像，demo 可选做

---

## 八、信息收集策略：三种约束来源

需求画像是多轮的核心载体，约束来源有三种，互为补充：

| 来源 | 谁提供信息 | 触发方式 | 示例 |
|---|---|---|---|
| 对话式（AI 引导） | 用户口述 | AI 主动提问 | "您目前英语是什么水平？" |
| 数据引导 | 业务反馈 | 候选数实时反馈 | "符合条件的课目前有 3 门" |
| **页面式（预填参数）** | 用户点选 | 页面选中数据随请求注入 | 点选"雅思基础班"卡片进入咨询 |

### 8.1 页面选数据 → 参数化注入（重点场景）

用户在前端页面点选一条数据（课程卡片/套餐卡片/年级），前端把该数据的**结构化描述**随消息一起发给 AI：

```
请求体：
{
  "session_id": "xxx",
  "user_msg": "适合我这种零基础的吗？",
  "selected_data": {
    "course_id": 3,
    "name": "雅思基础班",
    "price": 1999,
    "duration_days": 45,
    "level": "入门"
  }
}
```

AI 收到后：**把 selected_data 解析成结构化参数，注入需求画像**，作为初始约束。后续每一轮不再重复问"你想学什么"，只补剩余维度（时间、预算、偏好等）。

作用：**用最少的对话轮数把画像补到触发阈值**——用户已知的约束先交出来（快），AI 能查的补上（全），只有人知道的信息（预算/偏好）靠对话问（准）。

### 8.2 AI 引导的提问策略

- 哪个维度缺失问哪个（按画像缺口）
- 提问由**数据驱动**：用户没提预算但候选都超预算 → 触发"您有预算范围吗？"
- 每次只问一个关键维度，避免审问式连问

---

## 九、匹配与检索策略

### 9.1 结构化与文本分离

| 信息类型 | 存在哪 | 怎么查 | 谁写查询 |
|---|---|---|---|
| 课程名/年级/时长/教材 | 关系库 | 参数化查询 | 人工预写模板，AI 只填参数 |
| 定价/套餐/包月包年 | 关系库 | 参数化查询 | 同上 |
| 课程介绍/章节/段落内容 | 向量库 | 语义检索（chunk 带课程归属 metadata） | 索引时自动向量化 |

**为什么不让 AI 直接写 SQL**：表是自己设计的、字段确定；AI 现写 SQL 有幻觉表名、逻辑错误和注入风险。函数调用让 AI 输出结构化参数，后端用预写函数执行——这是 ragent、adaptive-rag-course 的一致做法。

### 9.2 混合检索（向量 + 关键词 + RRF）

- 向量检索：语义召回（"从零开始""基础差"这类软需求）
- 关键词检索（BM25 或 SQL LIKE/全文索引）：精确命中（"雅思""中考冲刺"）
- 融合：RRF（Reciprocal Rank Fusion）——业界事实标准

### 9.3 双层索引

- **课程摘要层**（粗路由）：课程名/简介/标签 → 先定位候选课程
- **段落 chunk 层**（精检索）：章节/单元/段落内容 → 精确定位知识点
- 每条 chunk 带 `course_id/chapter_id/unit_id` metadata，检索后可**上卷到课程**用于推荐与报价

### 9.4 意图路由 → 检索深度自适应

| 意图 | 检索深度 | top-k |
|---|---|---|
| chitchat | 不检索 | - |
| price/plan | 浅（SQL 精确） | - |
| content | 浅（向量，1 层） | 5 |
| recommend | 深（双层 + 混合） | 5-15 |

---

## 十、业务服务层：收窄过程与匹配结果中的核心帮助

业务服务不是"最后才查一次"的被动角色，而是整个多轮闭环的**另一只脚**：AI 负责"理解与引导"，业务层负责"反馈与执行"。业务层的帮助贯穿两个阶段——**收窄过程（每轮）** 和 **最终匹配结果（收口轮）**。

### 10.1 收窄过程中（每一轮）的业务帮助

收窄的本质：每轮用「完整需求画像」查询，候选范围逐步变小。业务层在每个环节提供 4 类帮助：

**① 约束反馈（让收窄可感知）**
每轮按当前画像返回候选数量 + 候选列表 → 用户能直观看到范围在缩小，愿意继续给约束。
> "按您的条件（英语、零基础），目前符合条件的课有 3 门：A、B、C。"
- 业务层动作：执行过滤查询，返回 `候选集 + 命中数 + 当前画像覆盖度`

**② 缺口提示（告诉 AI 还缺什么）**
业务层分析画像，发现缺失的关键维度 → 返回"缺口清单" → AI 据此决定问什么。
- 例：用户没提预算，但当前候选都是高价课 → 业务层提示"预算维度缺失，且候选均价 ¥3000+" → AI 追问"您有预算范围吗？"
- 例：用户没说水平 → 业务层提示"水平维度缺失，无法区分入门/进阶" → AI 问"您目前是什么水平？"
- 这实现了**数据驱动的引导**：不是 AI 随便问，而是业务层告诉 AI 哪个缺口最影响结果

**③ 冲突兜底（防答不上来）**
用户约束与库内数据冲突时，业务层先做可行性检查，返回替代方案而不是空结果。
- 例：用户要"30 天内学完"，但雅思基础班要 45 天 → 业务层返回"无直接命中，但『雅思速成套餐』28 天可完成，另有一门『雅思基础精简版』35 天" → AI 据此推荐替代
- 例：预算 500 但最便宜课程 1999 → 业务层返回"无命中 + 最接近的价位信息" → AI 如实说明并给选择

**④ 对比服务（帮用户在相近选项中决策）**
候选收窄到 2-3 个时，业务层生成**结构化对比数据**（价格/时长/教材/适合人群/完课率），AI 据此输出对比式回答。
> "A 课 30 天 1999 元，B 课 45 天 2599 元，A 更贴合您的时间要求。"
- 业务层动作：对候选集做字段对齐，输出对比矩阵 JSON

### 10.2 最终匹配结果过程中（收口轮）的业务帮助

当画像集齐、触发收口工具时，业务层提供最后一公里的 4 类帮助：

**① 排序与择优（决定"最终推荐谁"）**
业务层根据画像做**加权评分**：时间匹配度、难度匹配度、预算匹配度、课程评级、性价比（单位时长价格）→ 输出 Top-N 及排序理由。
- 例：用户"零基础 + 30 天 + 5000 预算" → 评分后"雅思基础班 92 分 > 雅思精简班 88 分 > 新概念英语 75 分"
- 业务层动作：`finalize_recommend` 按权重打分排序，附每项的命中理由

**② 报价生成（数字必须来自业务层）**
所有价格/时长/套餐金额由业务层从定价表计算输出，AI 只做表述——**从源头杜绝价格幻觉**。
- 单课报价：课程单价 + 可选套餐价 + 包月/包年折算价
- 套餐报价：套餐总价 vs 单课累加价，给出**节省金额**（业务层算，AI 展示）

**③ 组合推荐（跨表组装）**
业务层把「课程 + 套餐 + 时长 + 价格」跨表组装成完整方案，而不是让 AI 拼凑。
- 例：输出方案 = 雅思基础班（单课 ¥1999）+ 30 天速成套餐（¥3499，含 4 门配套课）→ 结构化 JSON 给 AI 和前端

**④ 下单前置校验（收口到业务闭环）**
用户确认后，业务层做库存/名额/状态校验并生成订单号，AI 只负责引导确认。
> "已为您锁定 1 个名额，订单号 CO-20260802-001，请在 15 分钟内支付。"

### 10.3 业务层帮助的整体定位

```
AI 负责：理解用户说什么（改写/意图/状态）
业务层负责：告诉 AI 库里有/没有、还缺什么、谁能匹配、值多少钱
AI 负责：把这些信息组织成用户能懂的回答和引导
```

**一句话**：业务层是"数据与逻辑的权威"，AI 是"表达与引导的窗口"。所有**影响结论的数据**（候选、缺口、冲突、评分、报价）都出自业务层，AI 不猜测任何业务事实——这正是多轮准确率和防幻觉的双重保证。

---

## 十一、工具清单与职责

工具不止在最后一步出现，而是**全程分工**。每个工具的本质：把"AI 能理解的结构化参数"翻译成"业务系统能执行的查询/动作"。

| 工具 | 触发场景 | 作用 | 入参（AI 填） | 出参 |
|---|---|---|---|---|
| `search_courses` 课程检索 | 推荐 | 按约束过滤课程候选池 | {科目,年级,难度,时长,课后} | 候选课程列表 + 数量 |
| `query_price` 价格查询 | 价格咨询 | 查单课/套餐价格 | {课程/套餐名} | 价格 + 时长 |
| `match_package` 套餐匹配 | 套餐/预算 | 匹配套餐 + 算性价比 | {目标课程,时长,预算} | 套餐列表 + 单价对比 |
| `search_content` 内容检索 | 内容咨询 | 查课程介绍/段落细节 | {课程,主题} | 命中段落 + 来源章节 |
| `check_duration` 时长匹配 | 规划 | 校验周期可行性 | {课程,目标天数} | 可/不可 + 替代建议 |
| `finalize_recommend` 收口推荐 | 画像就绪 | 输出最终 Top-N 方案 + 报价 | {完整画像} | 推荐方案 JSON |
| `create_order` 下单（demo 可模拟） | 用户确认 | 生成订单 | {课程/套餐,价格} | 订单号 + 确认信息 |

### 11.1 工具调用规范

- 每个工具：名称、描述、入参 schema（JSON Schema）→ 注册给 LLM 做 Function Calling
- AI 输出 `{tool: "search_courses", args: {...}}` → 后端路由到预写函数执行
- **AI 永远不直接碰数据库、不写 SQL**

---

## 十二、双层多轮：内部推理循环与可视化

### 12.1 内层多轮（AI ↔ 工具）

用户只发一句话，AI 在内部自规划、自循环多次调用工具后才回复：

```
用户："我想一个月内学完雅思基础，出个方案"
AI 内心：
  ① 规划：需要课程 + 时长 + 价格三类信息
  ② 调 search_courses → 拿到雅思基础班
  ③ 调 check_duration → 确认 30 天内可完课
  ④ 调 query_price → 拿到单课和套餐报价
  ⑤ 综合 → 输出推荐方案
```

业界称 **Agentic RAG / ReAct 循环**（Spring AI Alibaba ReactAgent、RAGFlow Agent 同款机制）。

### 12.2 全程可视化（页面可见）

后端 Agent 循环每执行一步产生一个**事件**，通过 **SSE 流**实时推给前端（边跑边播），前端渲染成思考过程面板：

```
┌─────────────────────────────────────────────┐
│  用户: "我想一个月内学完雅思基础，出个方案"      │
│                                             │
│  ▸ 思考过程（工具调用轨迹）                    │
│    ① 调「课程检索」→ 找到：雅思基础班           │
│    ② 调「时长匹配」→ 30天内可完课 ✓            │
│    ③ 调「价格查询」→ 单课 ¥1999 / 套餐 ¥3499  │
│    [3 步 · 1.2s]                             │
│                                             │
│  助手: "为您推荐雅思基础班，单课 1999 元…"      │
└─────────────────────────────────────────────┘
```

### 12.3 事件类型与前端渲染

| SSE 事件 | 含义 | 前端渲染 |
|---|---|---|
| `tool_start` | 开始调某工具 | 步骤卡片出现（工具名+图标+入参） |
| `tool_end` | 工具返回 | 卡片填入出参摘要 + 耗时 |
| `thinking` | AI 推理中间状态 | 思考气泡 |
| `answer` | 最终回答 | 正常流式打字机输出 |

### 12.4 业界参照（成熟套路，非创新）

- DeepSeek R1 / OpenAI o1 的思考过程展开区
- Cursor / Claude Code 的工具调用轨迹面板
- Dify / RAGFlow 的 Agent 思考过程面板
- Ant Design X 原生支持步骤/思维链类组件

---

## 十三、最终答案生成：查询产物 → 输出形态

### 13.1 数据链路

```
需求画像集齐
   → finalize_recommend（收口工具）
   → 输出结构化推荐方案 JSON
   → DeepSeek-V4-Flash（思考模式）基于方案 JSON + 检索证据生成自然语言回答
```

### 13.2 收口输出（结构化 JSON，前端可渲染成卡片）

```json
{
  "recommendation": [
    {
      "course": "雅思基础班",
      "price": 1999,
      "duration_days": 45,
      "fit_reason": "零基础入门，覆盖听说读写",
      "alternatives": ["雅思速成套餐 ¥3499 / 28 天"]
    }
  ],
  "summary": "根据您零基础、1 个月内完成的需求…",
  "next_actions": ["下单", "预约试听", "查看课表"]
}
```

### 13.3 答案形态（按场景）

| 场景 | 最终答案形态 |
|---|---|
| 课程推荐 | 推荐课程 + 理由 + 价格 |
| 价格咨询 | 价格表 + 可选套餐 |
| 内容咨询 | 知识点摘要 + 来源章节引用 |
| 套餐优惠 | 套餐对比 + 性价比结论 |
| 学习规划 | 可行性结论 + 替代方案 |
| 收口 | 完整方案 + 下单引导 |

### 13.4 防幻觉原则

- 回答必须基于检索结果（Groundedness）：只引用检索到的课程/价格/段落
- 查不到 → 明确说"库里没有这个信息"，不编造
- 价格类数字必须来自结构化查询结果，禁止模型"猜价格"

---

## 十四、完整演示剧本

### 14.1 纯对话式（从零咨询）

| 轮次 | 用户 | 场景路由 | 工具 | 画像变化 | 系统回复要点 |
|---|---|---|---|---|---|
| 1 | "我想学英语，有什么课？" | 推荐 | search_courses | {科目:英语} | 候选课程列表 |
| 2 | "我基础比较差，想从零开始" | 推荐 | search_courses + 缺口提示 | +{水平:零基础} | 候选收窄到入门课；AI 问时间 |
| 3 | "那大概要多少钱？" | 价格 | query_price | 不变 | 改写后查候选课价格 |
| 4 | "一个月能学完吗？" | 规划 | check_duration | +{时长≤30天} | 可行性 + 冲突兜底 |
| 5 | "套餐更划算的话推我一个" | 套餐 | match_package | +{套餐意向} | 套餐对比 + 性价比 |
| 6 | "就这个吧" | 收口 | finalize_recommend + create_order | 就绪 | 最终方案 + 订单 |

### 14.2 页面选数据式（预填参数）

| 步骤 | 用户动作 | 系统行为 |
|---|---|---|
| 0 | 页面点选「雅思基础班」卡片 + 输入"适合零基础吗" | selected_data 注入画像，AI 从已有约束继续 |
| 1 | 用户补充"想 1 个月学完" | 画像 +{时长≤30天}，check_duration |
| 2 | 用户补充"5000 以内" | 画像 +{预算}，match_package |
| 3 | 用户确认 | finalize_recommend + 下单 |

---

## 十五、业界共识五板斧对照

| # | 业界共识做法 | 本方案落地 |
|---|---|---|
| 1 | 查询改写/指代消解 | 五步管线第①步（非思考模式，低成本） |
| 2 | 意图路由控制检索深度 | 六类场景 + 检索深度自适应 |
| 3 | 约束累积 + 记忆分层 | 需求画像 + 滚动摘要（短期）+ 可选长期 |
| 4 | 混合检索 + 双层索引 | 向量 + 关键词 + RRF；课程摘要层 + 段落层 |
| 5 | SQL 参数化、AI 填参数 | 工具层预写函数，AI 函数调用输出参数 |

---

## 十六、数据分层与存储

### 16.1 业务实体（概念层）

- **分类科目**（英语/数学/语文…）
- **课程**：名称 / 教材 / 年级 / 是否课外 / 时长（天）/ 简介
- **章节**：章节名 / 介绍知识
- **单元**：单元标题
- **段落**：知识点内容 / 一句话摘要
- **定价体系**：单卖 / 套餐 / 包月 / 包年，各自含时长与覆盖课程
- **套餐-课程**：多对多映射

### 16.2 向量索引

- 只对**课程简介和段落**等自由文本建 chunk
- 每条 chunk 带 `course_id / chapter_id / unit_id` metadata
- 检索后上卷到课程 → 用于推荐与报价

### 16.3 会话存储

- session 表：session_id、需求画像 JSON、消息历史、滚动摘要、状态（收集中/就绪）

---

## 十七、风险与边界

| 风险 | 对策 |
|---|---|
| 意图误判 → 走错业务链 | 意图分类用明确 few-shot 示例；置信度低时按"推荐"兜底 |
| 查询改写跑偏 | 改写结果保留原问题，供模型对照 |
| 用户需求中途转向 | 约束累积不冻结候选集，随时可转向（画像整体替换目标） |
| 向量召回空结果 | 回退关键词检索；再空则引导用户换说法 |
| 冲突约束（如 30 天 vs 只有 45 天课） | 冲突兜底服务给替代方案，不硬推 |
| 价格幻觉 | 价格必须来自结构化查询结果，模型禁止自算 |
| 长对话 token 膨胀 | 滚动摘要压缩历史，控制成本 |

---

## 十八、Demo 里程碑拆分

| 阶段 | 内容 | 产出 |
|---|---|---|
| M1 | 数据库建库 + 种子数据（课程体系 + 定价 + 套餐） | SQLite 可查数据 |
| M2 | 向量索引构建（课程/段落 chunk + embedding + metadata） | ChromaDB 就绪 |
| M3 | 后端五步管线 + 六个工具 + 画像管理 | Spring AI 服务可对话 |
| M4 | 内层多轮可视化（SSE 事件流 + 思考面板） | 工具调用轨迹可见 |
| M5 | 页面选数据注入 + 收口推荐 + 下单模拟 | 完整闭环 |
| M6 | 演示剧本验证 + 边界打磨 | 可演示版本 |

---

> 本文档与 `调研报告_多轮智能问答开源项目.md` 配套使用。

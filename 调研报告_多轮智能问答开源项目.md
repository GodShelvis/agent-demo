# 多轮 AI 智能问答（课程推荐 / 知识库问答）开源项目调研报告

> 调研日期：2026-08-02
> 调研目的：梳理 GitHub 上"多轮对话 + 知识库问答（RAG/Agent）"相关的开源项目，统计**别人用的技术栈**和**处理步骤**，为我们的课程学习系统 AI 智能问答 demo 提供选型参考。

---

## 一、项目总览

| # | 项目 | Stars | 定位 | 语言/框架 | 数据库 | 多轮能力 |
|---|------|-------|------|-----------|--------|----------|
| 1 | [xqs-xqs/adaptive-rag-course](https://github.com/xqs-xqs/adaptive-rag-course) | 新 | PolyU 选课 RAG 聊天机器人（场景最贴近） | Python: FastAPI + LangChain | ChromaDB + SQLite | 多轮对话（session 历史） |
| 2 | [infiniflow/RAGFlow](https://github.com/infiniflow/ragflow) | 79.6K | 深度文档理解 RAG 引擎 + 工作流平台 | Python: FastAPI + React | Milvus + PostgreSQL + Redis + MinIO | 对话式知识库问答 |
| 3 | [langgenius/Dify](https://github.com/langgenius/dify) | 55K | 低代码/无代码 LLM 应用平台 | Python: Flask + React | PostgreSQL + pgvector | 多轮对话、Agent 编排 |
| 4 | [TencentCloud/Youtu-RAG](https://github.com/TencentCloud/Youtu-RAG) | 8K | Agentic RAG，智能客服，双层记忆 | Python: Youtu-Agent | ChromaDB + SQLite/MySQL + MinIO | 短期记忆 + 长期记忆，跨会话 |
| 5 | [HKUDS/LightRAG](https://github.com/HKUDS/LightRAG) | 20.3K | 轻量 RAG 框架 | Python | 支持本地存储/多种向量库 | 流式多轮对话、上下文压缩 |
| 6 | [nageoffer/ragent](https://github.com/nageoffer/ragent) | 新 | 生产级 Agentic RAG 平台（Java） | Java: Spring AI + Redis | MySQL + 向量库 | 会话记忆（最近 N 轮 + 持久化摘要） |
| 7 | [Mintplex-Labs/AnythingLLM](https://github.com/Mintplex-Labs/anything-llm) | 35K | 轻量个人/团队知识库问答 | Node.js + Python | SQLite + LanceDB | 多会话管理 |
| 8 | [SURESHBEEKHANI/LangGraph-End-to-End](https://github.com/SURESHBEEKHANI/LangGraph-End-to-End) | 教程 | LangGraph 从零构建 Agent（方法论） | Python: LangGraph | - | SelfRAG / AdaptiveRAG / CorrectiveRAG |
| 9 | [karthik-codex/Autogen_GraphRAG_Ollama](https://github.com/karthik-codex/Autogen_GraphRAG_Ollama) | 本地 | GraphRAG + 多智能体 + 全本地部署 | Python: AutoGen + Chainlit | Ollama + GraphRAG | 持续对话（Chainlit） |
| 10 | [DevYangJC/Argus](https://github.com/DevYangJC/Argus) | 新 | Spring AI Alibaba 知识库平台 | Java: Spring AI Alibaba + MyBatis-Plus | MySQL + 向量库 | Agent 图执行引擎 |

**闭源参考**：Alhena AI（电商购物多轮对话助手）——把多轮查询改写（Contextualizer）做到极致的商业案例。

---

## 二、重点项目详细拆解

### 2.1 xqs-xqs/adaptive-rag-course（最贴近我们的场景）

PolyU 选课 RAG 聊天机器人，帮学生从课程目录中选课，与"课程学习系统智能问答"高度同构。

**技术栈**
- 框架：FastAPI（后端）+ LangChain（RAG 编排）+ 原生 HTML/JS（前端，SSE 流式）
- LLM：Qwen-Plus（生成）、Qwen-Turbo（意图分类/查询扩展/分解）——**双模型分工策略**
- Embedding：DashScope text-embedding-v4（1536 维）
- 向量库：ChromaDB（HNSW 索引，嵌入式，SQLite 后端）
- 稀疏检索：rank-bm25 + jieba 中文分词
- 融合：Reciprocal Rank Fusion（RRF）
- 分词/预算：tiktoken

**核心步骤（管道）**
1. `txt_parser.py`：解析课程 TXT 文件（科目代码、标题、学分、大纲、考核方式等字段，模糊键匹配）
2. `chunking.py`：三层文档分块（课程摘要层 / 章节层 / chunk 层），parent-child chunking
3. `indexing.py`：Embedding 向量化 → 写入 ChromaDB + 构建 BM25 索引 + 生成课程摘要
4. `retrieval.py`：**自适应检索**
   - LLM 意图分类路由 4 条路径：chitchat（跳过）/ simple_lookup（直接检索）/ standard（扩展）/ complex（分解）
   - 双层索引：先课程级摘要索引粗路由 → 再 chunk 级索引精检索
   - 混合检索：向量 + BM25 → RRF 融合 → parent 回填 → 多样性过滤
5. `generation.py`：强模型生成回答，带行内引用 `[1]` `[2]`
6. `app.py`：FastAPI + SSE 流式输出；多轮对话通过 session 保存历史

**多轮处理方式**：session 级对话历史，查询时结合历史做意图分类和上下文理解。

**评估体系**：Hit Rate / Recall / MRR / NDCG（检索）+ Faithfulness / Groundedness / Completeness（生成），24 个测试用例分 4 类（简单查询 / 多课程 / 跨章节推理 / 防幻觉），并有消融实验对比 Naive RAG。

---

### 2.2 infiniflow/RAGFlow（深度文档理解 RAG 引擎）

企业级 RAG 平台，核心卖点是把"文档解析"做到极致，其余环节工作流化。

**技术栈**
- 微服务：FastAPI（API）+ React/Ant Design（前端）+ Worker（任务执行）
- 解析：DeepDoc 深度文档理解（布局感知、OCR、表格→Markdown）、支持 MinerU / PaddleOCR / 多模态 VLM
- 存储：Milvus（向量）+ PostgreSQL（元数据）+ Redis（任务队列）+ MinIO/S3（原始文件）
- 支持 15+ 种分块模板（Naive / Manual / Q&A / Table / Paper / Book / Laws / PPT / 图片等）

**核心步骤（数据摄入管线，0.21.0 后为可视化 Ingestion Pipeline）**
1. **Parser**：文档解析——识别布局、提取结构信息，得到半结构化内容
2. **Transformer**：分块（Chunker）与转换（Transformer）——模板化分块，可增删算子
3. **Indexer**：索引构建——多路召回架构，全文索引（BM25）+ 向量索引（Embedding）可并行配置

**检索阶段**
- 混合搜索：全文 + 向量，加权融合（约 5% 文本 + 95% 向量），可选 Rerank 模型重排
- TOC 引导检索（目录引导）、父子块检索（展开父块上下文）
- GraphRAG 支持：实体关系提取 → NetworkX 图 → 社区检测（Leiden）→ 图检索 + 向量检索双检索器并行，图结果优先

**对话环节**：创建"对话助手"（Chat）后开始问答，答案带来源引用。

---

### 2.3 langgenius/Dify（低代码 LLM 应用平台）

面向业务人员的可视化平台，把多轮对话、RAG、Agent、工作流全部可视化编排。

**技术栈**
- Python (Flask) + React/Next.js 前端
- PostgreSQL + pgvector 向量检索
- 支持主流 LLM 供应商、可编排工作流（LLM 节点、知识检索节点、工具节点、条件分支）

**典型步骤**
1. 上传文档 → 分块 → 向量化 → 建知识库（API 或拖拽）
2. 创建"应用"：选择对话型应用，绑定知识库
3. 编排工作流：问题理解节点（对话历史变量）→ 知识检索节点 → LLM 生成节点
4. 多轮对话：对话变量自动携带历史消息，可自定义"会话变量"累积状态

**价值点**：展示了"多轮对话 + 知识检索 + 工具调用"如何被低代码编排，适合快速原型参考。

---

### 2.4 TencentCloud/Youtu-RAG（Agentic RAG，双层记忆）

腾讯云开源的 Agent 驱动 RAG，重点解决传统 RAG 的三大短板：检索能力不足、数据隐私、记忆缺失。

**技术栈**
- 智能体框架：Youtu-Agent（自主规划、决策、调用工具、推理）
- 模型：Youtu-LLM / Youtu-Embedding / Youtu-Parsing / Youtu-HiChunk（可本地部署）
- 存储：ChromaDB（向量）+ SQLite/MySQL（关系型）+ MinIO（对象存储）
- 检索：多链路 Agent（关键词、向量、知识图谱等工具可选）

**核心设计**
1. **文件中心化架构**：以文件为中心，层级化、可增量编辑的多源异构数据管理
2. **智能检索引擎**：智能体自主决策检索策略（Query 改写、多跳检索、自我反思），多个开箱即用的 Agent 链路
3. **双层记忆机制**：
   - 短期记忆：大模型 Context Window 维护当前会话完整上下文 → 支持多轮指代消解、任务状态保持
   - 长期记忆：跨会话复用经验、个性化服务 → 从"无状态工具"进化为"有状态智能体"

**步骤**：数据入库（解析/分块/向量化）→ 构建知识库 → 配置 Agent 链路 → 多轮对话（短期记忆保持上下文，长期记忆跨会话）

---

### 2.5 HKUDS/LightRAG（轻量 RAG 框架）

香港大学数据智能系统实验室开源，几十行代码搭建 RAG 问答。

**技术栈**
- 纯 Python，pip 安装，最小依赖 < 50MB
- 检索：稠密向量（默认 e5-large-v2）+ BM25 稀疏检索 + 可选 ColBERT 交叉编码器重排
- 多源加载：PDF / Markdown / 网页 / Notion / API
- 兼容 OpenAI / Anthropic / HuggingFace 模型

**核心步骤**
1. `load()`：加载文档
2. 自动分块 → Embedding → 索引（增量索引：文档增删改实时生效）
3. `query()`：检索（Top-k + 重排）+ 生成
4. `chat()`：流式对话，支持多轮、上下文压缩（token 下降 30-50%）

**多轮处理方式**：流式对话 + 动态上下文压缩 + 分层检索（段落 → 句子 → token 三级粒度）。

---

### 2.6 nageoffer/ragent（生产级 Java Agentic RAG）

"后端程序员转型 AI 工程师第一站"的生产级 Java 平台，工程化程度高。

**技术栈**
- Java + Spring AI + Redis（队列/分布式控制）
- MySQL + 向量库 + MCP 工具支持

**核心设计**
1. **混合检索**：向量、关键词、知识图谱、联网搜索并行召回，去重 + RRF 融合 + Rerank
2. **问题理解**：查询词映射、问题重写与拆分、树形意图识别、多知识库路由
3. **会话记忆**：最近 N 轮消息 + 持久化摘要 → 控制 token 成本、保留关键上下文
4. **流量保护**：Redis 公平排队、分布式并发控制
5. **知识闭环**：可编排入库 Pipeline、远程刷新、回答溯源、用户反馈、Trace、管理后台

**多轮处理方式**：**"最近 N 轮完整保留 + 更早历史滚动压缩成摘要"**——这是多轮 token 成本控制的标准工程做法。

---

### 2.7 其余参考项目（简略）

| 项目 | 技术要点 |
|------|----------|
| AnythingLLM | Node.js 桌面/Web 应用，SQLite + LanceDB，文档上传即聊天，Docker 一键部署 |
| LangGraph-End-to-End | LangGraph 图谱化编排 Agent 的方法论教程：Agentic RAG（Agent 决定检索策略）、SelfRAG（自我反思生成质量）、CorrectiveRAG（检索结果纠错后重试）、AdaptiveRAG（按问题复杂度路由） |
| Autogen_GraphRAG_Ollama | GraphRAG + AutoGen 多智能体 + Ollama 本地模型 + Chainlit UI，全本地免费方案 |
| Argus | Spring AI Alibaba + ReactAgent 图执行引擎，Chat/Embedding 分离架构（Chat 走 DashScope 原生 API，Embedding 走 OpenAI 兼容） |

---

### 2.8 闭源参考：Alhena AI（多轮购物对话）

电商场景的多轮对话助手，核心是 **pre-retrieval 的查询改写管线（Contextualizer）**，对多轮 RAG 非常有借鉴意义。

**核心结论**：多轮对话里 **60% 以上的追问都包含指代或隐含上下文**（"那个要多少钱？"），直接拿原话检索会全部落空。

**四层并行改写（在 Embedding 之前执行）**
1. **ChatHistoryContextualizer**：把多轮对话折叠成一条自包含的独立问题（指代消解、隐含主语补齐），用便宜的小模型（GPT-4.1-nano）完成，降低延迟
2. **ProductSearchContextualizer**：用商品目录分类学 + 已累积的偏好重写查询
3. **QueryExpansionContextualizer**：生成改写变体增加召回多样性
4. **ProductsFilterContextualizer**：把硬约束（价格区间、库存、类目）提取为结构化过滤条件

**步骤**：每轮用户消息 → 并行改写（4 层）→ 用改写结果的**并集**去检索 → 生成。

**对我们最重要的启示**：检索质量的最大杠杆是"拿什么去检索"（改写后的查询），而不是检索器本身。

---

### 2.9 腾讯云 Agent Memory（记忆分层，2026 开源）

腾讯云数据库团队开源，四层渐进式记忆架构，公开评测显示长期记忆接入后回答准确率相对提升 ~59%（47.85% → 76.10%）。

**四层记忆架构（L0 → L3）**
- **L0 原始对话**：全量保存原文，随时可回查
- **L1 原子记忆**：自动提取事实、偏好、关键约束（JSONL 结构化记录，带标签）
- **L2 场景分块**：相关原子事实按项目/场景聚类（轻量摘要），精准召回不串场
- **L3 用户画像**：形成稳定用户画像（关键状态摘要）

**设计要点**
- 白盒可溯源：L3 结论 → L2 场景块 → L1 原子事实 → L0 原文，证据链不断裂
- 短期记忆压缩：工具调用完整结果卸载到外部文件，上下文只保留摘要 + 索引（实测 token 下降 33%-61%，任务完成率不降反升）
- 混合检索：Embedding 语义召回 + BM25 关键词召回 + RRF 融合

---

## 三、各项目技术栈对比汇总

| 环节 | 主流选择（被多个项目采用） |
|------|------------------------------|
| 后端框架 | FastAPI（Python 系绝对主流）、Spring AI（Java 系）、Flask（Dify） |
| RAG 编排 | LangChain / LlamaIndex / 自研 Agent 框架（Youtu-Agent、ReactAgent） |
| 生成模型 | Qwen-Plus、GPT 系列、Claude、DeepSeek（通过 OpenAI-compatible API 接入） |
| 意图/改写模型 | Qwen-Turbo 等**便宜小模型**（双模型分工是共识） |
| Embedding | text-embedding-v4、BGE、e5、nomic-embed-text（本地） |
| 向量库 | **ChromaDB（demo 首选）**、Milvus（生产）、pgvector（PG 插件）、FAISS、LanceDB |
| 关键词检索 | BM25（rank-bm25 + jieba 中文分词） |
| 结果融合 | RRF（Reciprocal Rank Fusion）为事实标准 |
| 重排 Rerank | BGE-Reranker、Cohere Rerank、ColBERT 交叉编码器 |
| 关系库 | SQLite（轻量/demo）、PostgreSQL/MySQL（生产） |
| 前端 | 原生 HTML/JS + SSE 流式（轻量）、React（平台级） |
| 对话记忆 | 会话历史 + 滚动摘要（ragent）、双层记忆（Youtu-RAG）、四层记忆（腾讯 Agent Memory） |
| 多轮核心优化 | **查询改写 / 指代消解**（全部项目共识）、意图路由、约束结构化过滤 |

---

## 四、多轮问答技术共性总结（业界共识做法）

综合以上项目，多轮问答提高准确率的技术手段可归纳为 5 层：

### 1. 查询改写 / 指代消解（最核心，成本最低收益最高）
- 每轮把「历史对话 + 当前问题」用**小模型**重写为一条**自包含的完整问题**
- 解决"那要多少钱？"这类追问的指代与省略问题
- 代表：Alhena Contextualizer、ragent 问题重写、RAGFlow GraphRAG 查询重写

### 2. 意图分类与路由（控制检索深度）
- LLM 判断问题类型：闲聊 / 简单查询 / 标准查询 / 复杂查询 / 价格 / 内容
- 按类型决定：是否检索、检索多深、走 SQL 还是向量（adaptive-rag-course 的四路路由）

### 3. 会话状态累积（多轮场景的杀手锏）
- 把每轮新出现的**结构化约束**（目标课程、年级、时长、预算）累积进会话状态
- 每轮检索用「改写后问题 + 完整约束集」，候选范围自动收窄
- 记忆分层是趋势：短期（当前会话）+ 长期（跨会话画像）

### 4. 混合检索 + 双层索引
- 向量（语义）+ BM25（关键词）双路召回 + RRF 融合，可选 Rerank
- 课程/文档摘要层粗路由 → 段落 chunk 层精检索（parent-child chunking）

### 5. 结构化数据与文本分离
- 价格、时长、套餐等**结构化数据走 SQL**（参数化查询），只有**自由文本才进向量库**
- SQL 生成策略：AI 通过函数调用输出**结构化参数**，后端用**预写参数化 SQL 模板**执行（防幻觉、防注入），而非让 AI 现场写 SQL

---

## 五、参考链接汇总

- [adaptive-rag-course（PolyU 选课 RAG）](https://github.com/xqs-xqs/adaptive-rag-course)
- [RAGFlow](https://github.com/infiniflow/ragflow)
- [Dify](https://github.com/langgenius/dify)
- [Youtu-RAG（腾讯）](https://github.com/TencentCloud/Youtu-RAG)
- [LightRAG（HKUDS）](https://github.com/HKUDS/LightRAG)
- [ragent（Java Agentic RAG）](https://github.com/nageoffer/ragent)
- [AnythingLLM](https://github.com/Mintplex-Labs/anything-llm)
- [LangGraph-End-to-End（方法论教程）](https://github.com/SURESHBEEKHANI/LangGraph-End-to-End)
- [Autogen_GraphRAG_Ollama（全本地多智能体）](https://github.com/karthik-codex/Autogen_GraphRAG_Ollama)
- [Argus（Spring AI Alibaba 知识库平台）](https://github.com/DevYangJC/Argus)
- [Alhena AI 博客：多轮购物对话的查询改写](https://alhena.ai/blog/query-rewriting-before-retrieval-multi-turn-rag/)
- [腾讯云 Agent Memory 技术剖析](https://cloud.tencent.com/developer/article/2709192)
- [Haystack 官方教程：Conversational RAG Agent](https://haystack.deepset.ai/tutorials/48_conversational_rag)
